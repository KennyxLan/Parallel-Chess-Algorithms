SIMPLE CHESS

A basic Chess AI using min-max search with alpha-beta pruning and a simplistic
approach to quiescent search.


-- COMPILE -- 

In `engine` folder, run `make`.


-- PLAY --

In `engine` folder, run `./chess`.
Enter your moves concatenating field names, e.g. 'b2b4'.


=======================================================
timeSerial notes:

1. Have two aiplayers competing with each other, and time each move.
2. For each move that is made, output the following in a csv file:
   - turn number,
   - color,
   - number of searched nodes,
   - time taken for that move
   - that move in plain human language

